from llm.openrouter_client import generate

def build_card(text):
    prompt = f"""
Ты senior-аналитик финтех-рынка.
Выдели ключевые события из сводки новостей финтех-рынка и сделай для каждой новости карточку:

Формат:

<Заголовок>
<Почему важно>
<Категория (регуляция / продукт / рынок / технологии)>
<Краткое резюме>
<Риски / возможности>

Новости:
{text}
"""

    return generate(prompt)